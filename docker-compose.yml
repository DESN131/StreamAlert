services:
  stream-alert:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stream-alert
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:5000:5000"